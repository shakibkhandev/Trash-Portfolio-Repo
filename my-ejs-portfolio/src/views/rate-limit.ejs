<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Rate Limit Exceeded - My Portfolio</title>
    <link rel="stylesheet" href="/styles.css" />
    <style>
      ::-webkit-scrollbar {
        display: none;
      }
      :root {
        --bg-primary: #ffffff;
        --bg-secondary: #f8f9fa;
        --text-primary: #1a1a1a;
        --text-secondary: #4a5568;
        --accent-primary: #dc2626;
        --accent-hover: #b91c1c;
        --border-radius: 16px;
        --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        --card-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1),
          0 2px 4px -1px rgba(0, 0, 0, 0.06);
      }

      body {
        background-color: var(--bg-primary);
        color: var(--text-primary);
        font-family: "Inter", -apple-system, BlinkMacSystemFont, "Segoe UI",
          Roboto, sans-serif;
        line-height: 1.6;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 20px;
        margin: 0;
        overflow-x: hidden;
      }

      .rate-limit-container {
        text-align: center;
        padding: clamp(20px, 5vw, 40px);
        max-width: min(600px, 90vw);
        width: 100%;
        animation: fadeIn 0.5s ease-out;
        box-sizing: border-box;
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .rate-limit-icon {
        width: clamp(60px, 8vw, 80px);
        height: clamp(60px, 8vw, 80px);
        margin: 0 auto clamp(16px, 3vw, 24px);
        animation: pulse 2s infinite;
      }

      @keyframes pulse {
        0% {
          transform: scale(1);
        }
        50% {
          transform: scale(1.05);
        }
        100% {
          transform: scale(1);
        }
      }

      .rate-limit-title {
        font-size: clamp(24px, 5vw, 28px);
        font-weight: 700;
        margin-bottom: clamp(12px, 2vw, 16px);
        color: var(--text-primary);
        background: linear-gradient(135deg, var(--accent-primary), #60a5fa);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
      }

      .rate-limit-message {
        font-size: clamp(14px, 3vw, 16px);
        color: var(--text-secondary);
        margin-bottom: clamp(24px, 4vw, 32px);
        max-width: min(500px, 85vw);
        margin-left: auto;
        margin-right: auto;
        line-height: 1.7;
      }

      .rate-limit-details {
        background-color: var(--bg-secondary);
        padding: clamp(16px, 4vw, 24px);
        border-radius: var(--border-radius);
        margin-bottom: clamp(24px, 4vw, 32px);
        max-width: min(500px, 85vw);
        margin-left: auto;
        margin-right: auto;
        text-align: left;
        box-shadow: var(--card-shadow);
        border: 1px solid rgba(0, 0, 0, 0.05);
        backdrop-filter: blur(10px);
      }

      .rate-limit-details h3 {
        margin-top: 0;
        color: var(--text-primary);
        font-size: clamp(16px, 3vw, 18px);
        margin-bottom: clamp(12px, 2vw, 16px);
        font-weight: 600;
      }

      .rate-limit-details p {
        margin-bottom: clamp(8px, 2vw, 12px);
        color: var(--text-secondary);
        font-size: clamp(13px, 2.5vw, 14px);
      }

      .rate-limit-details ul {
        padding-left: clamp(16px, 3vw, 20px);
        margin-bottom: clamp(12px, 2vw, 16px);
      }

      .rate-limit-details li {
        margin-bottom: clamp(6px, 1.5vw, 8px);
        color: var(--text-secondary);
        font-size: clamp(13px, 2.5vw, 14px);
      }

      .action-buttons {
        display: flex;
        justify-content: center;
        gap: clamp(12px, 2vw, 16px);
        margin-top: clamp(16px, 3vw, 24px);
        flex-wrap: wrap;
      }

      .action-button {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        background: linear-gradient(
          135deg,
          var(--accent-primary),
          var(--accent-hover)
        );
        color: white;
        padding: clamp(10px, 2vw, 12px) clamp(16px, 3vw, 24px);
        border-radius: var(--border-radius);
        text-decoration: none;
        transition: var(--transition);
        font-weight: 500;
        border: none;
        cursor: pointer;
        position: relative;
        overflow: hidden;
        box-shadow: var(--card-shadow);
        min-width: clamp(120px, 20vw, 140px);
        font-size: clamp(13px, 2.5vw, 14px);
        width: 100%;
        max-width: 200px;
      }

      .action-button:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 16px rgba(220, 38, 38, 0.2);
      }

      .action-button:active {
        transform: translateY(0);
      }

      @media (max-width: 480px) {
        body {
          padding: 12px;
        }

        .rate-limit-container {
          padding: 16px;
        }

        .rate-limit-details {
          padding: 16px;
        }

        .action-button {
          max-width: 100%;
        }
      }

      @media (min-width: 481px) and (max-width: 768px) {
        .rate-limit-container {
          padding: 24px;
        }
      }

      @media (min-width: 769px) {
        .rate-limit-container {
          padding: 32px;
        }
      }

      @media (orientation: landscape) and (max-height: 500px) {
        body {
          padding: 10px;
        }

        .rate-limit-container {
          padding: 16px;
        }

        .rate-limit-icon {
          width: 50px;
          height: 50px;
          margin-bottom: 12px;
        }

        .rate-limit-title {
          font-size: 20px;
          margin-bottom: 8px;
        }

        .rate-limit-message {
          font-size: 14px;
          margin-bottom: 16px;
        }

        .rate-limit-details {
          padding: 12px;
          margin-bottom: 16px;
        }

        .action-button {
          padding: 8px 16px;
        }
      }
    </style>
  </head>
  <body>
    <div class="rate-limit-container">
      <svg
        class="rate-limit-icon"
        viewBox="0 0 24 24"
        fill="none"
        xmlns="http://www.w3.org/2000/svg"
      >
        <path
          d="M12 22C17.5228 22 22 17.5228 22 12C22 6.47715 17.5228 2 12 2C6.47715 2 2 6.47715 2 12C2 17.5228 6.47715 22 12 22Z"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
        />
        <path
          d="M12 6V12L16 14"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
        />
      </svg>
      <h2 class="rate-limit-title">Rate Limit Exceeded</h2>
      <p class="rate-limit-message">
        You have made too many requests to our server. Please wait a moment
        before trying again.
      </p>

      <div class="rate-limit-details">
        <h3>What does this mean?</h3>
        <p>
          To ensure fair usage of our services, we limit the number of requests
          that can be made from a single IP address within a specific time
          period.
        </p>
        <p>Current limits:</p>
        <ul>
          <li>Regular pages: 100 requests per 15 minutes</li>
          <li>API endpoints: 30 requests per 15 minutes</li>
        </ul>
        <p>Please try again after the rate limit period has expired.</p>
      </div>

      <div class="action-buttons">
        <a href="javascript:history.back()" class="action-button">Go Back</a>
      </div>
    </div>
  </body>
</html>
